## Message queue setup

Run the rmq service and go to `localhost:15762`. Then import the definitions from `/rmq/definitions.json`. *TODO: Make the service durable so no imports needed*


## Citibike data

[Link to data](https://citibikenyc.com/system-data). Add data to `./data`